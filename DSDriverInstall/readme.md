### This utility will eventually provide a way to install dataSource drivers into JBoss EAP 6 as a module

Implemented drivers:
 * PostgresQL

To run:
 1. Clone the repo
 2. change into the DSDriverInstall directory
 3. run ./install_driver.sh -h to see command-line options
